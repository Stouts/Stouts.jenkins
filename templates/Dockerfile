# This file was generated by ansible for {{ansible_fqdn}}

FROM {{jenkins_image}}

# Install dependencies
{% if jenkins_apk_packages %}
USER root

RUN apk add --update {{ jenkins_apk_packages|join(' ') }}

USER jenkins
{% endif %}

# Install plugins
{% if jenkins_plugins %}
RUN /usr/local/bin/install-plugins.sh {{ jenkins_plugins|join(' ') }}
{% endif %}
